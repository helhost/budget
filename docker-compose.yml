services:
  api:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./server/data:/app/data

  client:
    image: nginx:alpine
    ports:
      - "3000:80"
    volumes:
      - ./client:/usr/share/nginx/html:ro
